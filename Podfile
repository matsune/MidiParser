# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
use_frameworks!

def shared_pods
  pod 'SwiftLint'
  pod 'SwiftFormat/CLI'
end

target 'MidiParser iOS' do
  shared_pods
end

target 'MidiParser macOS' do
  shared_pods
end
